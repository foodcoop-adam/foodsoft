- title 'My current order', false

- content_for :sidebar do
  = render 'edit_sidebar'

.page-header
  %h1
    %i.icon-shopping-cart.soft-icon
    = t 'group_orders.my_current_order'
    %span.normal= orders_status_line(@orders)

#articles_table
  - if @order_articles and !@order_articles.empty?
    = render 'order_articles'
  - else
    = t '.no_articles'

= render 'edit_javascript'
